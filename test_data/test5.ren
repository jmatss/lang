external function printf(format: {u8}, ...) -> i32

function fac(n: i32) -> i32
{
    if n <= 1
    {
        return 1
    }
    else
    {
        return n * fac(n - 1)
    }
}

function main() -> i32
{   
    var n: i32 = 7
    printf("n: %d, result: %d", n, fac(n))
    return 0
}