struct TestStruct<T>
{
    m: T,
}

impl TestStruct
{
    fn fn_a(value: u64) {}
    fn fn_b(value: T) {}
}

fn main() -> i32
{
    // NOT able to deduce type of `T` (unable to compile).
    TestStruct<i8>::fn_a(1)

    // Deduce type of `T` to i32 (default int deduced from the literal `1`).
    TestStruct<i8>::fn_b(1)

    return 0
}