use std.assert

struct TestStruct
{
    i: i32,
    msg: i32,
}

function main() -> i32
{
    var integer = 123
    var ptr = integer.&

    var ret = ptr.* + ptr.*
    assert(ret == 246)
    
    return ret
}