use std::StringView

fn main() -> i32
{
    var view = StringView::new("test output")
    //var res

// TODO:
/*
    res = std::print_new(view)
    std::assert(res.&.is_success())

    res = std::print_new(view, line_break=false)
    std::assert(res.&.is_success())

    res = std::print_new(view, line_break=false, flush=true)
    std::assert(res.&.is_success())
*/

    return 0
}
