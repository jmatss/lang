ext fn printf(format: {u8}, ...) -> i32

struct TestStruct {
    var i: i32
    var msg: i32
}

fn main() -> i32 {
    var integer = 123
    var ptr = integer.&

    var ret = ptr.* + ptr.*
    printf("ret: %d", ret)
    //std::assert(ret == 246)

    return 0
}