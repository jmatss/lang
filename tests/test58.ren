fn test_fn(x: u8) -> u8 {
    return x
}

fn main() -> i32 {
    var res1: f32 = if false {
        yield 1.0
    } else {
        yield 0.0
    }
    // std::assert(res1 == 0.0)

    var cond = 2
    var res2 = test_fn(if cond == 2 { yield 1 } else { yield 0 })
    //std::assert(res2 == 1)
}