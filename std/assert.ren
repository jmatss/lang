mod std

use std::StringView

ext fn exit(exit_code: i32)

fn assert(expr: bool, msg: StringView = @null()) {
    if not expr {
        std::print("\nASSERT FAILED")
        if not @is_null(msg.ptr) {
            std::print(msg)
        }
        exit(1)
    }
}
